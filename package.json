{
  "name": "chrome-themes-ranked",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Scrape Chrome Web Store theme ratings and publish a sortable static site.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "scrape": "node scripts/scrape.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "playwright": "1.63.0"
  },
  "engines": {
    "node": ">=20"
  }
}
